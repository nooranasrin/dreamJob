type Query {
  jobs: [Job]!
  job(id: ID!): Job!
  company(id: ID!): Company!
}

scalar Date

type Company {
  _id: ID!
  name: String!
}

type Location {
  country: String!
  city: String
}

enum EmploymentType {
  FULL_TIME
  PART_TIME
  CONTRACT
}

type Experience {
  start: Int!
  end: Int!
}

type Job {
  _id: ID!
  role: String!
  company: Company!
  location: Location!
  description: String!
  experience: Experience
  postedTime: Date
  noOfVacancy: Int
  employmentType: EmploymentType
  tags: [String]
}
